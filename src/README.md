# Source Directory

This directory previously contained the embedding pipeline implementation. The backend implementation has now been moved to the `backend/` directory for better organization.

The main embedding pipeline code is now located at:
- `backend/src/main.py`

See the backend directory for the complete implementation.